Usage
=====

Launch the TUI session manager::

    telix

Connect directly to a host::

    telix mud.example.com 4000

All ``telnetlib3`` client flags are passed through (``--encoding``, ``--ssl``,
``--loglevel``, etc.).  Run ``telix --help`` for the full list.
